// Shared configuration will be exported from here
export const DEFAULT_CONFIG = {
  autoHideDuration: 10000,
  overlayPosition: 'bottom-right' as const,
  sampleRate: 16000,
  bitDepth: 16,
  channels: 1,
};
