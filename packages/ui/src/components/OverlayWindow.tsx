import React from 'react';

export interface OverlayWindowProps {
  message: string;
  visible: boolean;
  autoHideDuration?: number;
}

export const OverlayWindow: React.FC<OverlayWindowProps> = () => {
  // Component implementation will be added in task 9.1
  return null;
};
