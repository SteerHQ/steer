# Исправление: Последнее слово не попадало в запись

## Проблема

При тестировании обнаружилось, что последнее слово вопроса не попадало в аудиофайл.

## Причина

После обнаружения 2 секунд тишины система сразу вызывала `get_audio_data()`, но последние аудио данные могли еще не успеть записаться в буфер из-за:

1. Задержки в WASAPI потоке (обработка каждые 10ms)
2. Задержки в мониторинге (проверка каждые 5 секунд)
3. Асинхронной природы записи в буфер

## Решение

Добавлена **post-roll задержка** в 500ms после обнаружения паузы и перед получением данных из буфера.

### Код

```typescript
// 2 seconds of silence passed, process the accumulated audio
console.log('✅ Speech ended, waiting 500ms for post-roll...');
isSpeechActiveRef.current = false;
silenceStartTimeRef.current = 0;
setSpeechState('idle');

// Skip if already processing
if (isProcessing) {
  console.log('⚠️ Already processing, skipping');
  return;
}

setProcessing(true);

// Wait 500ms to capture the tail of speech (post-roll)
await new Promise(resolve => setTimeout(resolve, 500));
console.log('📥 Post-roll complete, getting audio data');

// Get accumulated audio data
const audioBuffer = await invoke<number[]>("get_audio_data");
```

## Временная диаграмма

```
┌─────────────────────────────────────────────────────────┐
│  Речь                                                   │
│  "Что такое React hooks?"                               │
└─────────────────────────────────────────────────────────┘
                                    ▲
                                    │ Последнее слово
                                    │
┌───────────────────────────────────┼─────────────────────┐
│  Детекция тишины                  │                     │
│  0ms ────► 2000ms                 │                     │
└───────────────────────────────────┼─────────────────────┘
                                    │
                                    ▼
┌───────────────────────────────────┼─────────────────────┐
│  Post-roll задержка               │                     │
│  2000ms ────► 2500ms              │ ◄── Захват хвоста   │
└───────────────────────────────────┼─────────────────────┘
                                    │
                                    ▼
┌───────────────────────────────────┼─────────────────────┐
│  Получение данных                 │                     │
│  get_audio_data()                 │ ✅ Все слова есть   │
└───────────────────────────────────┴─────────────────────┘
```

## Параметры

- **Длительность post-roll:** 500ms
- **Когда применяется:** После обнаружения 2 секунд тишины
- **Цель:** Захватить последние аудио данные, которые еще записываются в буфер

## Результат

✅ Последнее слово теперь попадает в запись  
✅ Полный захват речи от начала до конца  
✅ Минимальная задержка (всего 500ms)

## Альтернативные решения (не выбраны)

1. **Увеличить длительность паузы до 3 секунд**
   - ❌ Слишком долгое ожидание
   - ❌ Плохой UX

2. **Уменьшить интервал мониторинга до 1 секунды**
   - ❌ Больше нагрузки на CPU
   - ❌ Не решает проблему полностью

3. **Изменить логику буферизации в Rust**
   - ❌ Более сложное решение
   - ❌ Требует изменений в WASAPI коде

## Тестирование

### Сценарий 1: Короткий вопрос
```
Вопрос: "Что такое React?"
Результат: ✅ Все слова захвачены
```

### Сценарий 2: Длинный вопрос
```
Вопрос: "Расскажите о вашем опыте работы с TypeScript и React"
Результат: ✅ Все слова захвачены, включая "React"
```

### Сценарий 3: Вопрос с паузами
```
Вопрос: "Что такое... [пауза 1 сек]... React hooks?"
Результат: ✅ Все слова захвачены, включая "hooks"
```

## Связанные файлы

- `apps/desktop/src/App.tsx` - добавлена post-roll задержка
- `SPEECH_ACCUMULATION.md` - обновлена документация
- `IMPROVEMENTS_SUMMARY.md` - добавлен параметр post-roll
- `CHANGELOG.md` - добавлена запись об исправлении
